# frozen_string_literal: true

RSpec.describe LtiCore do
  it "has a version number" do
    expect(LtiCore::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
